# C5 — behavior diagnosis across unrelated candidates

Fix the session-expiry behavior reported by the tests. Several modules expose
similarly named helpers, so repository evidence must identify the responsible implementation.
