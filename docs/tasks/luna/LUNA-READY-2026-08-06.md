# Luna Readiness Packet — 2026-08-06

Handoff from **DeepSeek V4 Flash / DS-001 + DS-002** (both integrated into `main`).
Prepared by the lead architect so Luna can start immediately.

## 1. Current main baseline

- **Commit:** `2bf86e8` — `merge: integrate DS-002 isolated Worker runtime`
- **History:** `2bf86e8` ← `270714d` (DS-002) ← `50d4c1f` (DS-001 merge) ← `ef5e3a9` (DS-001) ← `f7cdc3e` (foundation)
- `pnpm check` is green under Node 24 (format / lint / typecheck / test / build).
- Branch from **this** main:
  - `agent/luna-ui-001-foundation` (UI-001), then after review+merge
  - `agent/luna-ui-002-core-flow` (UI-002)

## 2. What Luna owns

- `apps/desktop/src/renderer/**`
- `apps/desktop/components.json` only if a shadcn component add requires it
- `pnpm-lock.yaml` only as generated by an already approved UI dependency

**Do not edit:** main/preload, `packages/domain`, `packages/contracts`,
`packages/persistence`, `packages/worker-runtime`, ADRs, product baselines,
`docs/tasks/**`.

## 3. Integrated backend surfaces (read-only reference)

- `@canvas-agent/domain` — source of truth for `TaskStatus`, `RunStatus`,
  `RunOutcome`, `BaselineStatus`, `SnapshotStatus`, `SnapshotFreshness`,
  `ApprovalStatus`, Node/Edge types and the invariant functions. Domain badges must
  import these types; never duplicate status color logic per page.
- `@canvas-agent/contracts` — `runtimeInfoSchema` / `window.canvasAgent.getRuntimeInfo()`
  for the desktop health API; `ExecutionRequest`/`repositoryRevision` schema that your
  Run fixtures mirror.
- `packages/persistence` + `packages/worker-runtime` — implemented and integrated.
  The renderer must **not** import them (no Node/filesystem/process access). They only
  confirm the data model your mock fixtures should faithfully mirror.

## 4. Current renderer state (already on main)

```
apps/desktop/src/renderer/src/
  App.tsx  main.tsx  index.html  env.d.ts
  assets/main.css                      (Tailwind v4 + CSS variables)
  lib/utils.ts
  hooks/use-runtime-info.ts
  data/musicdb-fixture.ts              (typed MUSICDB fixture)
  components/app/app-shell.tsx
  components/domain/{status-badge,status-tone}.tsx(+test)
  components/ui/{badge,button}.tsx
```

Visual references: the four PNG files in `canvas_agent_design_baseline_v1.1/`.

## 5. Order of work

1. **UI-001** on `agent/luna-ui-001-foundation` — complete the UI foundation and
   component gallery (AppShell, sidebar, header, inspector, page toolbar, empty/
   loading/error states, domain badges, theme toggle, Cmd/Ctrl+K palette).
2. Request architecture review; push branch; do not merge locally across computers.
3. After UI-001 is merged, **UI-002** on `agent/luna-ui-002-core-flow` — the MUSICDB
   core-flow prototype (Dashboard → Task → Context Composer → Run → Artifact/Baseline).

## 6. Verification gates

- **UI-001:** desktop `lint`, `typecheck`, `test`, `build`; capture **1440×1080** and
  **1180×820** screenshots in both light/dark themes; include file paths in the handoff.
- **UI-002:** desktop `lint`, `typecheck`, component tests, `build`; interaction tests
  for acceptance criteria 2–6; capture the five core flow states at **1440×1080**.

```bash
pnpm --filter @canvas-agent/desktop lint
pnpm --filter @canvas-agent/desktop typecheck
pnpm --filter @canvas-agent/desktop test
pnpm --filter @canvas-agent/desktop build
```

Use Node 24: `PATH=/opt/homebrew/opt/node@24/bin:$PATH pnpm ...`

## 7. Hard constraints (do not regress)

- Renderer has no Node.js / filesystem / database / Git / process access; never expose
  raw IPC or Electron objects from components.
- Status must use color + text/icon/shape (WCAG AA); never color alone.
- Keyboard-first with visible focus; light/dark parity.
- Do not turn screenshots into hardcoded layouts or fake business state; fixtures stay
  typed and clearly fixture-only.
- Every formal transition (freeze / accept / complete / activate) is a **separate,
  explicit** user action — no chained automation.
- If a contract (domain / contracts / main-preload) blocks you, stop and open a short
  proposal instead of editing those files.

## 8. Handoff contract (return per `docs/tasks/README.md`)

1. commit SHA and branch name;
2. modified file list;
3. short implementation explanation;
4. exact commands run and results;
5. acceptance-criterion evidence;
6. unresolved questions and risks;
7. explicit disclosure of any scope deviation.

**Plus:** UI-001 → component inventory + token-change table.
UI-002 → screen-state map showing every explicit formal transition.
