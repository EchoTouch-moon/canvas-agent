# Product MVP v0.2 RC verification

- **Status:** VERIFIED LOCALLY AND IN CI — LEAD APPROVED
- **RC task baseline:** `main@29b5425`
- **Branch:** `agent/deepseek-ds-007-product-mvp-rc`
- **Runtime:** Node `v24.15.0`, pnpm `11.9.0`
- **Platform:** macOS (Apple Silicon)
- **Merged baseline:** `main@38820ec` via PR #13
- **GitHub Actions:** run `31450943361` — `check` PASS, `macos-electron` PASS

This evidence index supports the lead architect's Product MVP v0.2 completion decision. Generated CI artifacts are not committed; local default paths are listed below and CI overrides them into the uploaded `ci-artifacts/` tree.

## Evidence contract

| Scenario                                                     | Invocation                                                                                                  | Binary observable                                                                                                                                                                                                                                                                            | Captured artifact path                                                                                                 |
| ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| Workspace invalid/cancel/choose/reopen/switch/package picker | `pnpm --filter @canvas-agent/desktop e2e:workspace`                                                         | exit 0 and `ALL PASSED`; invalid directory returns `ERROR/NOT_GIT_WORKTREE` with no active workspace                                                                                                                                                                                         | `apps/desktop/dist/logs/workspace-lifecycle.log` via aggregate RC suite                                                |
| Packaged Agent launcher selection                            | packaged `.app` within `pnpm --filter @canvas-agent/desktop e2e:workspace`                                  | `agent.chooseExecutable` through the E2E-only native selection seam returns `READY / USER_SELECTED / codex-cli 0.146.0`; subsequent `agent.status` returns the same committed READY state                                                                                                    | `apps/desktop/dist/logs/workspace-lifecycle.log`                                                                       |
| Complete domain loop and final restart                       | `pnpm --filter @canvas-agent/desktop e2e:live`                                                              | exit 0 and `ALL PASSED`; Run → evaluation → completion → apply → DRAFT → activation; launch C reads durable ACTIVE Baseline, APPLIED revision and FINISHED Run                                                                                                                               | `apps/desktop/dist/logs/full-loop-restart-adoption.log`; `apps/desktop/dist/screenshots/live/final-restart-active.png` |
| Adoption retry idempotency                                   | within `e2e:live`                                                                                           | exact binding returns same application; Git HEAD and full commit count unchanged; `repository_revision` count unchanged                                                                                                                                                                      | `apps/desktop/dist/logs/full-loop-restart-adoption.log`                                                                |
| Packaged cold start/migrations                               | `pnpm --filter @canvas-agent/desktop e2e:packaged-smoke`                                                    | exit 0; isolated userData reaches READY; migration/core tables exist; missing resource exits 1 without app tables                                                                                                                                                                            | `apps/desktop/dist/logs/packaged-cold-start.log`                                                                       |
| Credential-free aggregate                                    | `pnpm e2e:rc`                                                                                               | exit 0; report `status=passed`, `credentialFree=true`, four scenarios passed                                                                                                                                                                                                                 | `apps/desktop/dist/reports/product-mvp-v0.2-rc.json` plus `apps/desktop/dist/logs/*.log`                               |
| Authenticated Agent smoke                                    | disabled: `pnpm --filter @canvas-agent/desktop e2e:agent`; opt-in: `CANVAS_AGENT_REAL_AGENT_SMOKE=1 ...`    | JSON contains executable version, exactly one executed/skipped/failed count, and stable `checks` keys: `agentReady`, `dispatchSucceeded`, `patchProduced`, `structuredSummary`, `workerDiffCheckPassed`, `sourceRepositoryUnchanged`; executed PASS requires all true, skip records all null | `apps/desktop/dist/reports/agent-smoke.json`                                                                           |
| Source quality                                               | `pnpm check`                                                                                                | exit 0                                                                                                                                                                                                                                                                                       | `apps/desktop/dist/logs/check.log`; CI `source-check-logs` on failure                                                  |
| Dependency audit                                             | `pnpm audit --prod --audit-level high`; authorized mirror fallback: `--registry=https://registry.npmjs.org` | exit 0, no high/critical production vulnerability                                                                                                                                                                                                                                            | `apps/desktop/dist/logs/audit-prod.log`; CI `ci-artifacts/logs/audit-prod.log` on failure                              |

## Local verification results

| Invocation                                                                             | Result                                                                                                                                                                                |
| -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `node --check` for all changed E2E CommonJS files                                      | PASS                                                                                                                                                                                  |
| Prettier check for changed E2E/package/workflow files                                  | PASS                                                                                                                                                                                  |
| `pnpm --filter @canvas-agent/desktop build`                                            | PASS                                                                                                                                                                                  |
| `pnpm --filter @canvas-agent/desktop build:unpack:unsigned`                            | PASS — packaged macOS app rebuilt with signing identity disabled                                                                                                                      |
| `pnpm --filter @canvas-agent/desktop e2e:workspace`                                    | PASS — cancel, invalid, valid, reopen, switch, packaged repository picker and packaged Agent executable picker; committed Agent status is `READY / USER_SELECTED / codex-cli 0.146.0` |
| `pnpm --filter @canvas-agent/desktop e2e:live`                                         | PASS — all three launches and adoption retry assertions                                                                                                                               |
| disabled `pnpm --filter @canvas-agent/desktop e2e:agent`                               | PASS as a truthful skip report: `outcome=skipped`, version `codex-cli 0.146.0`, `redactedReason=OPT_IN_DISABLED`                                                                      |
| `pnpm e2e:rc`                                                                          | PASS — JSON reports `status=passed`, `credentialFree=true`; all four scenarios exit 0                                                                                                 |
| `pnpm check`                                                                           | PASS — format/lint/typecheck; 470 tests (5 + 56 + 68 + 90 + 251); build PASS                                                                                                          |
| `pnpm audit --prod --audit-level high --registry=https://registry.npmjs.org`           | PASS — `No known vulnerabilities found`; the configured npmmirror endpoint did not implement audit, so the lead explicitly authorized the official registry fallback                  |
| opt-in `CANVAS_AGENT_REAL_AGENT_SMOKE=1 pnpm --filter @canvas-agent/desktop e2e:agent` | PASS — `outcome=executed`, counts `1/0/0`, `codex-cli 0.146.0`; all six non-sensitive checks are `true`                                                                               |

The authenticated execution result is a separate opt-in gate and is never inferred from the credential-free deterministic suite or the disabled-smoke result.
The report never includes the raw prompt, stdout/stderr, repository path or Run identifier. Failed runs preserve observed `true`/`false` values and leave checks that were not reached as `null`.

## Lead decision

All blocking rows in the master acceptance matrix are closed. PR #13 passed both GitHub Actions jobs at the reviewed head `28de779`; the merge commit is `38820ec`. Signing and notarization are deferred only for a future external-distribution release and do not block the local/internal unsigned Product MVP v0.2.
