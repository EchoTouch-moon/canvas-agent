# `@canvas-agent/worker-runtime`

Reserved ownership: **DeepSeek V4 Flash / DS-002** after DS-001 is integrated.

This package will validate immutable `ExecutionRequest` contracts, create isolated Git worktrees, launch one configured Agent CLI without a shell, and return patch/test/summary artifacts.

See `docs/tasks/deepseek/DS-002-worker-runtime.md` before editing.
