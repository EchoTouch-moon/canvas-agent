# `@canvas-agent/persistence`

Reserved ownership: **DeepSeek V4 Flash / DS-001**.

This package will implement SQLite schema, migrations, repositories, transaction boundaries and content-addressed Blob metadata. It must depend on `@canvas-agent/domain`; it must not import Electron or renderer code.

See `docs/tasks/deepseek/DS-001-persistence-foundation.md` before editing.
